# Dependencies
node_modules

# Build output
dist
dist-ssr

# Local env — never commit real credentials. `.env.example` is the template.
*.local
.env
.env.*
!.env.example

# Vite / TS caches and stray compiled config
.vite
*.tsbuildinfo
vite.config.js
vite.config.d.ts

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Stray Flutter files that were committed here by mistake — this is the
# React admin console, not the app.
/shield agent_invester/
